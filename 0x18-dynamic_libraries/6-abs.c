#include "main.h"
#include <stdlib.h>

/**
 *_abs()- computes the absolute value of an integer
 *
 *@j: parameter passed through the function
 *
 * Return: abs() value
 */

int _abs(int j)
{
	return (abs(j));
}
