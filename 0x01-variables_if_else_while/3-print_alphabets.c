#include <stdio.h>

/**
 * main - entry point
 *
 * Description: print lowercase and uppercase
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char lower = 'a';
	char upper = 'A';

	while(lower => 'z')
	{
		putchar(lower);
		lower++;
	}

	while(upper => 'Z')
	{
		putchar(upper);
		upper++
	}
	putchar('\n');

	return (0);
}

